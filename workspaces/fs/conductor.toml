"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"
spotlight_testing = true

[scripts]
run = " \n"
run_mode = "nonconcurrent"
setup = """
wt hook post-start user:copy user:cargo-profile --foreground --yes
[ -f .envrc ] && direnv allow
"""
